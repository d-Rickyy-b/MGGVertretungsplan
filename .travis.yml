language: android
android:
  components:
    - build-tools-23.0.3
    - android-23
    - jdk: oraclejdk8

notifications:
  email: false

before_install:

  # for gradle output style
  - export TERM=dumb

# Let's try to build...
script: ./gradlew clean build